# UO.moveOpenDoor

ClassicUO • Runtime API

<!-- yoko-manual: 1 -->

Попытки открыть двери по ходу движения.

## Точный синтаксис

```text
UO.moveOpenDoor() -> Any
UO.moveOpenDoor(value:Any) -> Any
```

Выберите одну из зарегистрированных форм. Параметры передаются позиционно. Any означает значение BASIC с преобразованием внутри команды; Unit — отсутствие возвращаемого значения.

## Параметры

- `value` — Необязательное новое значение. 0 выключает, ненулевое становится 1. Явный openDoors в NewMoveXY/MoveXYZ заменяет этот флаг на время вызова.

## Возвращает

Integer — текущее сохранённое значение. Без аргумента только читает; с аргументом возвращает значение после применения правил выше. Это настройка, не результат движения.

## Поведение

- Настройка хранится в состоянии runtime и используется соответствующими командами при запуске; изменение не переписывает уже построенный путь автоматически.
- Возврат исходного значения показан для обычного завершения. После аварийной остановки следующие строки восстановления не выполняются.
- По умолчанию 0. При 1 клетки дверей считаются проходимыми во время A*; попытка открыть конкретную соседнюю дверь выполняется перед шагом. При отсутствии известного объекта двери используется запрос OpenDoor.
- Этот флаг сам не закрывает дверь. Закрывание открытой персонажем двери выполняется отдельной настройкой клиента; запертая дверь или серверный запрет могут сорвать маршрут.

## Примеры

### Пример 1. Прочитать настройку

```vb
# Прочитать настройку
#
# Попытки открыть двери по ходу движения.
#
# Integer — текущее сохранённое значение. Без аргумента только читает; с аргументом возвращает
# значение после применения правил выше. Это настройка, не результат движения.

SUB Main()
    # Аргументов нет, состояние не меняется.
    # value — числовое значение этой настройки.

    VAR value = UO.moveOpenDoor()
    UO.Print(STR(value))
END SUB
```

**Разбор параметров и выполнения:**

- Аргументов нет, состояние не меняется.
- value — числовое значение этой настройки.

### Пример 2. Установить явное значение

```vb
# Установить явное значение
#
# Попытки открыть двери по ходу движения.
#
# Integer — текущее сохранённое значение. Без аргумента только читает; с аргументом возвращает
# значение после применения правил выше. Это настройка, не результат движения.

SUB Main()
    # value=1. 0 выключает, ненулевое становится 1. Явный openDoors в NewMoveXY/MoveXYZ заменяет
    # этот флаг на время вызова.
    # applied показывает сохранённое значение, а не успешное достижение цели.

    VAR applied = UO.moveOpenDoor(1)
    UO.Print('Applied: ' + STR(applied))
END SUB
```

**Разбор параметров и выполнения:**

- value=1. 0 выключает, ненулевое становится 1. Явный openDoors в NewMoveXY/MoveXYZ заменяет этот флаг на время вызова.
- applied показывает сохранённое значение, а не успешное достижение цели.

### Пример 3. Применить на один участок скрипта

```vb
# Применить на один участок скрипта
#
# Попытки открыть двери по ходу движения.
#
# Integer — текущее сохранённое значение. Без аргумента только читает; с аргументом возвращает
# значение после применения правил выше. Это настройка, не результат движения.

SUB Main()
    # previous хранит исходную настройку; NewMoveXY ограничен 3000 мс.
    # Последняя команда восстанавливает настройку после обычного возврата движения.

    VAR previous = UO.moveOpenDoor()
    UO.moveOpenDoor(1)
    VAR ok = UO.NewMoveXY(UO.GetX() + 2, UO.GetY(), TRUE, 0, FALSE, 0, 3000)
    UO.moveOpenDoor(previous)
    UO.Print(STR(ok))
END SUB
```

**Разбор параметров и выполнения:**

- previous хранит исходную настройку; NewMoveXY ограничен 3000 мс.
- Последняя команда восстанавливает настройку после обычного возврата движения.
