# UO.Race

ClassicUO • Runtime API

<!-- yoko-manual: 1 -->
<!-- yoko-locale: ru -->

Читает расу персонажа из модели клиента.

## Точный синтаксис

```text
UO.Race() -> Any
```

## Параметры

Параметров нет.

## Возвращает

Integer — 1=human, 2=elf, 3=gargoyle по модели клиента. 0 означает отсутствие загруженного персонажа. Это не body/graphic и не serial.

## Поведение

- Читаются уже полученные локальные данные. Запрос не открывает status и не заставляет сервер прислать обновление. После смерти, перехода карты или удаления объекта значения могут измениться между двумя вызовами.
- Это функции чтения: присваивание результата переменной сохраняет снимок. Оно не изменяет характеристику персонажа. Имена нечувствительны к регистру; используйте именно показанную форму с нужным числом аргументов.

## Примеры

### Вывести текущее значение своего персонажа

```vb
# Вывести текущее значение своего персонажа
#
# Читает расу персонажа из модели клиента.
#
# Integer — 1=human, 2=elf, 3=gargoyle по модели клиента. 0 означает отсутствие загруженного
# персонажа. Это не body/graphic и не serial.

SUB Main()
    # Вызов без аргументов относится к self. Расу персонажа сохраняется в value.
    # STR преобразует число для вывода и не меняет смысл результата.

    VAR value = UO.Race()
    UO.Print('Race: ' + STR(value))
END SUB
```

**Разбор параметров и выполнения:**

- Вызов без аргументов относится к self. Расу персонажа сохраняется в value.
- STR преобразует число для вывода и не меняет смысл результата.

### Использовать значение в прикладной проверке

```vb
# Использовать значение в прикладной проверке
#
# Читает расу персонажа из модели клиента.
#
# Integer — 1=human, 2=elf, 3=gargoyle по модели клиента. 0 означает отсутствие загруженного
# персонажа. Это не body/graphic и не serial.

SUB Main()
    # Значение читается один раз без аргументов. Расу персонажа используется в показанном расчёте
    # или условии.
    # Числовые пороги — пример условий скрипта, а не новые пределы клиента. Поле сохраняет серверные
    # единицы и ограничения, описанные выше.

    VAR value = UO.Race()
    IF value = 3 THEN
        UO.Print('Player race is gargoyle')
    END IF
END SUB
```

**Разбор параметров и выполнения:**

- Значение читается один раз без аргументов. Расу персонажа используется в показанном расчёте или условии.
- Числовые пороги — пример условий скрипта, а не новые пределы клиента. Поле сохраняет серверные единицы и ограничения, описанные выше.

### Наблюдать изменение за одну секунду

```vb
# Наблюдать изменение за одну секунду
#
# Читает расу персонажа из модели клиента.
#
# Integer — 1=human, 2=elf, 3=gargoyle по модели клиента. 0 означает отсутствие загруженного
# персонажа. Это не body/graphic и не serial.

SUB Main()
    # Оба вызова без аргумента читают своё текущее значение. WAIT(1000) даёт паузу в миллисекундах
    # между снимками.
    # Сравнивается изменение: расу персонажа. Пример ничего не изменяет в мире и не ждёт бесконечно.
    # События внутри этой секунды могут быть пропущены.

    VAR before = UO.Race()
    WAIT(1000)
    VAR after = UO.Race()
    IF before <> after THEN
        UO.Print('Before: ' + STR(before) + '; after: ' + STR(after))
    END IF
END SUB
```

**Разбор параметров и выполнения:**

- Оба вызова без аргумента читают своё текущее значение. WAIT(1000) даёт паузу в миллисекундах между снимками.
- Сравнивается изменение: расу персонажа. Пример ничего не изменяет в мире и не ждёт бесконечно. События внутри этой секунды могут быть пропущены.
