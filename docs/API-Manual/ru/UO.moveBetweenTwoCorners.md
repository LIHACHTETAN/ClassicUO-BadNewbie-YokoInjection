# UO.moveBetweenTwoCorners

ClassicUO • Runtime API

<!-- yoko-manual: 1 -->
<!-- yoko-locale: ru -->

Разрешение диагонали при двух заблокированных боковых клетках.

## Точный синтаксис

```text
UO.moveBetweenTwoCorners() -> Any
UO.moveBetweenTwoCorners(value:Any) -> Any
```

## Параметры

- `value` — Необязательное новое значение. 0 запрещает, ненулевое становится 1. Сервер может отвергнуть такой проход независимо от клиентской настройки.

## Возвращает

Integer — текущее сохранённое значение. Без аргумента только читает; с аргументом возвращает значение после применения правил выше. Это настройка, не результат движения.

## Поведение

- Настройка хранится в состоянии runtime и используется соответствующими командами при запуске; изменение не переписывает уже построенный путь автоматически.
- Возврат исходного значения показан для обычного завершения. После аварийной остановки следующие строки восстановления не выполняются.

## Примеры

### Прочитать настройку

```vb
# Прочитать настройку
#
# Разрешение диагонали при двух заблокированных боковых клетках.
#
# Integer — текущее сохранённое значение. Без аргумента только читает; с аргументом возвращает
# значение после применения правил выше. Это настройка, не результат движения.

SUB Main()
    # Аргументов нет, состояние не меняется.
    # value — числовое значение этой настройки.

    VAR value = UO.moveBetweenTwoCorners()
    UO.Print(STR(value))
END SUB
```

**Разбор параметров и выполнения:**

- Аргументов нет, состояние не меняется.
- value — числовое значение этой настройки.

### Установить явное значение

```vb
# Установить явное значение
#
# Разрешение диагонали при двух заблокированных боковых клетках.
#
# Integer — текущее сохранённое значение. Без аргумента только читает; с аргументом возвращает
# значение после применения правил выше. Это настройка, не результат движения.

SUB Main()
    # value=0. 0 запрещает, ненулевое становится 1. Сервер может отвергнуть такой проход независимо
    # от клиентской настройки.
    # applied показывает сохранённое значение, а не успешное достижение цели.

    VAR applied = UO.moveBetweenTwoCorners(0)
    UO.Print('Applied: ' + STR(applied))
END SUB
```

**Разбор параметров и выполнения:**

- value=0. 0 запрещает, ненулевое становится 1. Сервер может отвергнуть такой проход независимо от клиентской настройки.
- applied показывает сохранённое значение, а не успешное достижение цели.

### Применить на один участок скрипта

```vb
# Применить на один участок скрипта
#
# Разрешение диагонали при двух заблокированных боковых клетках.
#
# Integer — текущее сохранённое значение. Без аргумента только читает; с аргументом возвращает
# значение после применения правил выше. Это настройка, не результат движения.

SUB Main()
    # previous хранит исходную настройку; NewMoveXY ограничен 3000 мс.
    # Последняя команда восстанавливает настройку после обычного возврата движения.

    VAR previous = UO.moveBetweenTwoCorners()
    UO.moveBetweenTwoCorners(0)
    VAR ok = UO.NewMoveXY(UO.GetX() + 2, UO.GetY(), TRUE, 0, FALSE, 0, 3000)
    UO.moveBetweenTwoCorners(previous)
    UO.Print(STR(ok))
END SUB
```

**Разбор параметров и выполнения:**

- previous хранит исходную настройку; NewMoveXY ограничен 3000 мс.
- Последняя команда восстанавливает настройку после обычного возврата движения.
