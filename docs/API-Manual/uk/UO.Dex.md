# UO.Dex

ClassicUO • Runtime API

<!-- yoko-manual: 1 -->
<!-- yoko-locale: uk -->

Читає поточну спритність (Dexterity, DEX) персонажа.

## Точний синтаксис

```text
UO.Dex() -> Integer
```

## Параметри

Параметрів немає.

## Повертає

Integer — поточні очки характеристики, 0..65535 у моделі клієнта; не відсоток, ID, навичка, режим блокування чи Boolean. 0 також означає відсутнього/видаленого персонажа або недоступний об’єкт. Порівнюйте з числовою вимогою, не = TRUE. Це не ліміт характеристики й не гарантоване базове значення без модифікаторів.

## Поведінка

- Команда не приймає аргументів. Використовуйте точні сигнатури вище.
- Читає Player.Dexterity, якщо Player існує й не знищений; інакше 0. Наявний мертвий персонаж не є видаленим об’єктом. Характеристика не обчислюється з HP, мани чи витривалості.
- GetStr/GetInt/GetDex приймають ObjID, але клієнт зберігає ці характеристики лише у PlayerMobile: будь-який інший serial дає 0, навіть для завантаженого mobile. Це обмеження щодо загального опису Stealth; чужа характеристика не вгадується.
- Invoke читає на ігровому потоці; очікування можна скасувати разом зі скриптом. Не запитує status, не відкриває target, не надсилає пакета, не змінює характеристику й не додає затримки.
- Кожен виклик читає локальні дані заново. Змінна зберігає знімок; окремі читання можуть бачити різні оновлення сервера. Ненульовий результат не перевіряє з’єднання. Нуль буває значенням або відсутністю даних.
- Рівнозначні імена характеристики з UO. та без нього, без урахування регістру: `Dex Dexterity GetDex GetDexterity`.
- Int(value) без UO. округлює BASIC-число вниз, Str(value) без UO. форматує значення в текст. Це інші операції, ніж UO.Int()/UO.Str(), які читають характеристики. GetInt(ObjID) не округлює числа.

### Внутрішні функції: від виклику до результату

Нижче фактичні етапи читання. AttributeAtLeast — повністю визначена користувацька BASIC-функція прикладу, не прихована API чи зміна характеристики.

#### 1. RegisterCharacterGetterAliases

RegisterCharacterGetterAliases реєструє відсутні функції без аргументів і вбудовані імена. Наявні гілки сумісності обирають свій getter; обидва шляхи повертають Integer. Ім’я без дужок читається заново, якщо його не перекрила змінна.

Рівнозначні імена характеристики з UO. та без нього, без урахування регістру: `Dex Dexterity GetDex GetDexterity`.

Код проєкту: `external/InjectionScript/src/InjectionScript/Runtime/InjectionApiUO.cs`; функція `RegisterCharacterGetterAliases`.

#### 2. Invoke

Читає Player.Dexterity, якщо Player існує й не знищений; інакше 0. Наявний мертвий персонаж не є видаленим об’єктом. Характеристика не обчислюється з HP, мани чи витривалості. Invoke читає на ігровому потоці; очікування можна скасувати разом зі скриптом. Не запитує status, не відкриває target, не надсилає пакета, не змінює характеристику й не додає затримки.

Integer — поточні очки характеристики, 0..65535 у моделі клієнта; не відсоток, ID, навичка, режим блокування чи Boolean. 0 також означає відсутнього/видаленого персонажа або недоступний об’єкт. Порівнюйте з числовою вимогою, не = TRUE. Це не ліміт характеристики й не гарантоване базове значення без модифікаторів. Кожен виклик читає локальні дані заново. Змінна зберігає знімок; окремі читання можуть бачити різні оновлення сервера. Ненульовий результат не перевіряє з’єднання. Нуль буває значенням або відсутністю даних.

Код проєкту: `src/ClassicUO.Client/Game/Managers/ClassicUOInjectionApiBridge.cs`; функція `Invoke`.

#### 3. CharacterStatus

CharacterStatus записує отримане поле STR/DEX/INT у Player.Dexterity під час обробки відповідного status-пакета власного персонажа. Команди читають цей кеш і не чекають нового пакета.

Invoke читає на ігровому потоці; очікування можна скасувати разом зі скриптом. Не запитує status, не відкриває target, не надсилає пакета, не змінює характеристику й не додає затримки.

Код проєкту: `src/ClassicUO.Client/Network/PacketHandlers.cs`; функція `CharacterStatus`.

#### 4. Clear

World.Clear прибирає Player. Далі повертається 0, доки не з’являться поточний персонаж і його дані. Збережене значення не доводить виконання вимоги після перепідключення.

Integer — поточні очки характеристики, 0..65535 у моделі клієнта; не відсоток, ID, навичка, режим блокування чи Boolean. 0 також означає відсутнього/видаленого персонажа або недоступний об’єкт. Порівнюйте з числовою вимогою, не = TRUE. Це не ліміт характеристики й не гарантоване базове значення без модифікаторів.

Код проєкту: `src/ClassicUO.Client/Game/World.cs`; функція `Clear`.

World.Clear прибирає Player. Далі повертається 0, доки не з’являться поточний персонаж і його дані. Збережене значення не доводить виконання вимоги після перепідключення.


## Приклади

### Прочитати й вивести очки

```vb
# Прочитати й вивести очки
#
# Читає поточну спритність (Dexterity, DEX) персонажа.
#
# Integer — поточні очки характеристики, 0..65535 у моделі клієнта; не відсоток, ID, навичка,
# режим блокування чи Boolean. 0 також означає відсутнього/видаленого персонажа або недоступний
# об’єкт. Порівнюйте з числовою вимогою, не = TRUE. Це не ліміт характеристики й не гарантоване
# базове значення без модифікаторів.

SUB Main()
    # value зберігає числовий результат; CStr лише форматує його для журналу. Аргументів і дії
    # персонажа немає.

    VAR value = UO.Dex()
    UO.Print('Dexterity: ' + CStr(value))
END SUB
```

**Пояснення параметрів і виконання:**

- value зберігає числовий результат; CStr лише форматує його для журналу. Аргументів і дії персонажа немає.

### Порівняти два знімки

```vb
# Порівняти два знімки
#
# Читає поточну спритність (Dexterity, DEX) персонажа.
#
# Integer — поточні очки характеристики, 0..65535 у моделі клієнта; не відсоток, ID, навичка,
# режим блокування чи Boolean. 0 також означає відсутнього/видаленого персонажа або недоступний
# об’єкт. Порівнюйте з числовою вимогою, не = TRUE. Це не ліміт характеристики й не гарантоване
# базове значення без модифікаторів.

SUB Main()
    # before та after розділяє 1000 мс; WAIT додано до прикладу. change=after-before може бути
    # додатним, нульовим або від’ємним. Сам він не розрізняє всі проміжні оновлення й вихід зі
    # світу.

    VAR before = UO.Dex()
    WAIT(1000)
    VAR after = UO.Dex()
    VAR change = after - before
    UO.Print('Change: ' + CStr(change))
END SUB
```

**Пояснення параметрів і виконання:**

- before та after розділяє 1000 мс; WAIT додано до прикладу. change=after-before може бути додатним, нульовим або від’ємним. Сам він не розрізняє всі проміжні оновлення й вихід зі світу.

### Повна функція перевірки вимоги

```vb
# Повна функція перевірки вимоги
#
# Читає поточну спритність (Dexterity, DEX) персонажа.
#
# Integer — поточні очки характеристики, 0..65535 у моделі клієнта; не відсоток, ID, навичка,
# режим блокування чи Boolean. 0 також означає відсутнього/видаленого персонажа або недоступний
# об’єкт. Порівнюйте з числовою вимогою, не = TRUE. Це не ліміт характеристики й не гарантоване
# базове значення без модифікаторів.

SUB Main()
    # minimum=80 — приклад вимоги, не ліміт клієнта. AttributeAtLeast(minimum) відхиляє відсутнього
    # персонажа, один раз читає характеристику й повертає Integer Boolean: 1=TRUE або 0=FALSE за
    # умовою >= minimum. Логічний результат дає порівняння, не сама характеристика.

    IF AttributeAtLeast(80) = TRUE THEN
        UO.Print('Requirement met')
    ELSE
        UO.Print('Requirement not met or no player')
    END IF
END SUB

SUB AttributeAtLeast(minimum)
    IF UO.Self() = 0 THEN
        RETURN FALSE
    END IF
    VAR value = UO.Dex()
    RETURN value >= minimum
END SUB
```

**Пояснення параметрів і виконання:**

- minimum=80 — приклад вимоги, не ліміт клієнта. AttributeAtLeast(minimum) відхиляє відсутнього персонажа, один раз читає характеристику й повертає Integer Boolean: 1=TRUE або 0=FALSE за умовою >= minimum. Логічний результат дає порівняння, не сама характеристика.
