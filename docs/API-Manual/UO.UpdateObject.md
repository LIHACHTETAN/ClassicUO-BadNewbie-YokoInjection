# UO.UpdateObject

ClassicUO • Runtime API • `UO.UpdateObject.md`

Для загруженного персонажа запрашивает обновлённый статус. LastStatus меняется при принятии ответа, не при отправке запроса. Для загруженного предмета отправляет одиночный клик. Данные приходят асинхронно; неизвестный serial сам по себе не загружает произвольный объект мира.

## Точный синтаксис / Registered signatures

```text
UO.UpdateObject(id:Any) -> Unit
```

Порядок аргументов соответствует строкам выше. `Unit` означает отсутствие возвращаемого значения. `Any` — значение BASIC с преобразованием при вызове. Имена нечувствительны к регистру.

## Варианты использования

Это отдельные сценарии. Подставьте свои serial, пути и координаты; серверные действия зависят от текущего состояния игры.

### Прямой вызов

```vb
SUB Main()
    UO.UpdateObject(self)
END SUB
```

### Условный вызов из игрового скрипта

```vb
SUB Main()
    IF UO.Connected THEN
        VAR arg1 = self # id
        UO.UpdateObject(arg1)
    END IF
END SUB
```

### Повторное использование через процедуру

```vb
SUB RunAction()
    VAR arg1 = self # id
    UO.UpdateObject(arg1)
END SUB

SUB Main()
    # Запуск процедуры выполняет действие:
    RunAction()
END SUB
```

## Реализация для проверки поведения

- `InjectionScript.Runtime.InjectionApiUO.UpdateObject`

## Возвращает

Unit — команда не возвращает значение. Вызывайте её отдельной строкой. Не используйте её результат как serial, type или логический признак успеха. Завершение вызова само по себе не подтверждает выполнение действия сервером.
