name: Export .NET SDK 10.0.302

on:
  push:
    branches:
      - ClientUO-Age-of-Power
    paths:
      - _buildsdk/READY
      - .github/workflows/export-dotnet-sdk.yml

permissions:
  contents: read

jobs:
  export-sdk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install .NET SDK 10.0.302
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.302'

      - name: Pin and verify SDK 10.0.302
        shell: bash
        run: |
          set -euo pipefail
          cat > global.json <<'EOF'
          {"sdk":{"version":"10.0.302","rollForward":"disable","allowPrerelease":false}}
          EOF
          test "$(dotnet --version)" = "10.0.302"
          dotnet --info

      - name: Package SDK core and packs
        shell: bash
        run: |
          set -euo pipefail
          TMP="$RUNNER_TEMP"
          CORE="$TMP/dotnet-core-10.0.302"
          PACKS="$TMP/dotnet-packs-10.0.302"
          mkdir -p "$CORE/sdk" "$PACKS"
          cp /usr/share/dotnet/dotnet "$CORE/"
          cp -a /usr/share/dotnet/host "$CORE/"
          cp -a /usr/share/dotnet/sdk/10.0.302 "$CORE/sdk/"
          cp -a /usr/share/dotnet/shared "$CORE/"
          cp -a /usr/share/dotnet/packs "$PACKS/"
          if [ -d /usr/share/dotnet/sdk-manifests ]; then cp -a /usr/share/dotnet/sdk-manifests "$PACKS/"; fi
          if [ -d /usr/share/dotnet/templates ]; then cp -a /usr/share/dotnet/templates "$PACKS/"; fi
          if [ -d /usr/share/dotnet/metadata ]; then cp -a /usr/share/dotnet/metadata "$PACKS/"; fi
          tar -C "$TMP" -cJf "$TMP/dotnet-core-10.0.302-linux-x64.tar.xz" dotnet-core-10.0.302
          tar -C "$TMP" -cJf "$TMP/dotnet-packs-10.0.302-linux-x64.tar.xz" dotnet-packs-10.0.302
          sha256sum "$TMP/dotnet-core-10.0.302-linux-x64.tar.xz" > "$TMP/dotnet-core-10.0.302-linux-x64.tar.xz.sha256"
          sha256sum "$TMP/dotnet-packs-10.0.302-linux-x64.tar.xz" > "$TMP/dotnet-packs-10.0.302-linux-x64.tar.xz.sha256"
          split -b 70M -d -a 2 "$TMP/dotnet-core-10.0.302-linux-x64.tar.xz" "$TMP/dotnet-core-10.0.302-linux-x64.part"
          test "$(find "$TMP" -maxdepth 1 -name 'dotnet-core-10.0.302-linux-x64.part*' -type f | wc -l)" -ge 2
          for part in "$TMP"/dotnet-core-10.0.302-linux-x64.part*; do
            test -s "$part"
            sha256sum "$part"
          done
          ls -lh "$TMP"/dotnet-core-10.0.302-linux-x64.part* "$TMP/dotnet-packs-10.0.302-linux-x64.tar.xz"

      - name: Upload SDK core part 00
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-core-10.0.302-part00
          path: |
            ${{ runner.temp }}/dotnet-core-10.0.302-linux-x64.part00
            ${{ runner.temp }}/dotnet-core-10.0.302-linux-x64.tar.xz.sha256
          compression-level: 0
          if-no-files-found: error
          retention-days: 1

      - name: Upload SDK core part 01
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-core-10.0.302-part01
          path: ${{ runner.temp }}/dotnet-core-10.0.302-linux-x64.part01
          compression-level: 0
          if-no-files-found: error
          retention-days: 1

      - name: Upload SDK core part 02
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-core-10.0.302-part02
          path: ${{ runner.temp }}/dotnet-core-10.0.302-linux-x64.part02
          compression-level: 0
          if-no-files-found: error
          retention-days: 1

      - name: Upload SDK core part 03
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-core-10.0.302-part03
          path: ${{ runner.temp }}/dotnet-core-10.0.302-linux-x64.part03
          compression-level: 0
          if-no-files-found: error
          retention-days: 1

      - name: Upload SDK core part 04
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-core-10.0.302-part04
          path: ${{ runner.temp }}/dotnet-core-10.0.302-linux-x64.part04
          compression-level: 0
          if-no-files-found: error
          retention-days: 1

      - name: Upload SDK core part 05
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-core-10.0.302-part05
          path: ${{ runner.temp }}/dotnet-core-10.0.302-linux-x64.part05
          compression-level: 0
          if-no-files-found: error
          retention-days: 1

      - name: Upload SDK core part 06
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-core-10.0.302-part06
          path: ${{ runner.temp }}/dotnet-core-10.0.302-linux-x64.part06
          compression-level: 0
          if-no-files-found: ignore
          retention-days: 1

      - name: Upload SDK core part 07
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-core-10.0.302-part07
          path: ${{ runner.temp }}/dotnet-core-10.0.302-linux-x64.part07
          compression-level: 0
          if-no-files-found: ignore
          retention-days: 1

      - name: Upload SDK packs artifact
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-packs-10.0.302-linux-x64
          path: |
            ${{ runner.temp }}/dotnet-packs-10.0.302-linux-x64.tar.xz
            ${{ runner.temp }}/dotnet-packs-10.0.302-linux-x64.tar.xz.sha256
          compression-level: 0
          retention-days: 1
